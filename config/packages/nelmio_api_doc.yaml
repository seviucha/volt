nelmio_api_doc:
    documentation:
        info:
            title: Github repo stats fetcher
            description: Fetch github repo stats & compare
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
